Flask==3.0.0
Flask-CORS==4.0.0
requests==2.31.0
beautifulsoup4==4.12.2
python-dotenv==1.0.0
gunicorn==21.2.0
wolframalpha==5.0.0
assemblyai==0.44.3

# spaCy with pre-built wheels
spacy==3.7.2
blis==0.7.11

# Speech recognition (optional - may fail on some platforms)
# Comment out if deployment fails
pyttsx3==2.90
SpeechRecognition==3.10.0

# PyAudio (optional - comment out if fails)
# pyaudio==0.2.14
